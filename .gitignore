# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# Node / Astro
node_modules/
site/dist/
site/.astro/
.vercel/

# Raw data pulls are reproducible; do not version them.
data-pipeline/data/raw/*
!data-pipeline/data/raw/.gitkeep

# OS
.DS_Store
Thumbs.db

.vercel
.env*

# Committed data contract is CSV; parquet is a local convenience only.
data-pipeline/data/processed/*.parquet

# Local agent scratch DBs - these lock on Windows and have broken a rebase here before.
.claude/data/
