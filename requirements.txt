# India FS Pulse - data pipeline
# Verified against Python 3.14.5 on 2026-08-20.
pandas>=2.2
requests>=2.32
yfinance>=0.2.40
pyarrow>=17.0        # parquet; pipeline degrades to CSV if the wheel is unavailable
numpy>=1.26
pillow>=10.0         # social card only, site/scripts/make_og.py
python-docx>=1.1       # docs/build_report.py, the .docx deliverable
